## 基本约定
#### 方向规定
俯视时，从几何中心向核心板的方向是前进的方向，四个电机分别为lf,lb,rf,rb  
lf,rb电机顺时针转;rf,lb电机逆时针转  
实测在TOF超距时返回值为8192，建议大于1400时直接置为1400  
当前imu安装方向为水平向前为x，水平向右为y，竖直向下为z  
定义坐标系为水平向前为x，水平向右为y，竖直向下为z  

pitch：机头向上，即前段高后端低时为正，此时应表示为向后飞，反之为负  
roll:左端高右端低为正，此时应表示向右飞，反之为负  
yaw:向顺时针为正，逆时针为负[已修改，符合右手系]  
实测前倾与右倾时相应符合逻辑  

图传画面上方为前进方向，当前相机垂直向下  
MT9V03X_H MT9V03X_W 120 * 188  
cam_down.centers[0][0]表示从上到下数的行数，在最上方为0  
cam_down.centers[0][1]表示从左到右数的列数，在最左方为0  
0-------------------------------------------------------188  
|  
|  
|  
|  
|  
|  
|  
|  
120  
其中行数小为目标在前，列数小为目标在左  

xx_ground_pos计算结果中，以无人机投影为原点，无人机的前进方向在地面的投影方向为x轴正方向，无人机右侧方向在地面的投影方向为y轴正方向  

#### 控制逻辑
现在通过“视觉环->角度环->角速度环”三串级结构控制飞行状态  

#### 屏幕使用
屏幕以输出文字和数字信息为主。ips200_show_xxx函数中，前两个参数代表位置的x,y坐标,  
如有最后一个参数，通常代表字符长度。一个字符大约占据x方向10个像素，y方向16个像素。  
https://github.com/but0n/Avem.git  
                                                   
### 传输数组

#### `share_data_from_1` (Core 1 写入，Core 0 读取)

该数组用于 Core 1 传递给 Core 0。

| 索引 | 内容描述 | 来源/用途 |
| :--- | :--- | :--- |
| [0] | cam_down.centers[0][0] (Car Y) | 视觉：识别到的第一个目标（小车）的像素 Y 坐标 |
| [1] | cam_down.centers[0][1] (Car X) | 视觉：识别到的第一个目标（小车）的像素 X 坐标 |
| [2] | (float)cam_down.dot_num[0] (Car Area) | 视觉：识别到的第一个目标（小车）的像素点数量（面积）。若无目标则为 0。 |
| [3] | car_ground_pos.x | 计算：小车在地面坐标系中的 X 坐标 (右侧) |
| [4] | car_ground_pos.y | 计算：小车在地面坐标系中的 Y 坐标 (前方) |
| [5] | target_ground_pos.x | 计算：第二个目标在地面坐标系中的 X 坐标 (右侧) |
| [6] | target_ground_pos.y | 计算：第二个目标在地面坐标系中的 Y 坐标 (前方) |
| [7] | ray_car.x | 调试：小车在相机坐标系中的射线 X 分量 (右) |
| [8] | ray_car.y | 调试：小车在相机坐标系中的射线 Y 分量 (前) |
| [9] | ray_car.z | 调试：小车在相机坐标系中的射线 Z 分量 (上/负下) |
| [10] | body_car.x | 调试：小车在机体坐标系中的射线 X 分量 (右) |
| [11] | body_car.y | 调试：小车在机体坐标系中的射线 Y 分量 (前) |
| [12] | body_car.z | 调试：小车在机体坐标系中的射线 Z 分量 (上/负下) |
| [13] | distance(car, target) | 计算：小车与目标在地面坐标系下的欧几里得距离 |
| [14] | cam_down.light_number | 计算：检测到的有效灯数 |
| [15] | 未使用 | |

#### `share_data_from_0` (Core 0 写入，Core 1 读取)

该数组用于 Core 0 传递给 Core 1。

| 索引 | 内容描述 | 来源/用途 |
| :--- | :--- | :--- |
| [0] | imu_data.roll | IMU：横滚角 (Roll) |
| [1] | imu_data.pitch | IMU：俯仰角 (Pitch) |
| [2] | imu_data.yaw | IMU：偏航角 (Yaw) |
| [3] | imu_data.z | IMU：Z 轴位置 (高度) |
| [4] | (float)current_drone_state |调试：无人机状态|
| [5] | motor_out.lf | CONTROL：左前输出值|
| [6] | motor_out.rf | CONTROL：右前输出值|
| [7] | motor_out.lb | CONTROL：左后输出值|
| [8] | motor_out.rb | CONTROL：右后输出值|
| ... | ... | |
| [15] | 未使用 | |

#### `uart_data`


| 索引 | 内容描述 | 来源/用途 |
| :--- | :--- | :--- |
| 0 | car_ground_pos.x | 计算：小车在地面坐标系中的 X 坐标 |
| 1 | car_ground_pos.y | 计算：小车在地面坐标系中的 Y 坐标 |
| 2 | target_ground_pos.x | 计算：目标在地面坐标系中的 X 坐标 (若未识别到目标则为 0) |
| 3 | target_ground_pos.y | 计算：目标在地面坐标系中的 Y 坐标 |
| 4 | imu_data.yaw | IMU：无人机的偏航角 (Yaw)，来自 Core 0 共享数据 |
| 5 |cam_down.light_number | 计算：检测到的有效灯数 |
| 6 | |  |
| 7 | |  |

## 更新日志
**1.12**  
v1.0  

**1.15**  
v2.0  
修改了串级PID中的逻辑性问题，优化了IMU，PWM,视觉部分加入了非线性控制  
现在M7_0负责飞行控制，M7_1负责图像处理，实测图像处理部分处理频率为4.55ms  
修改了大量小bug  

**1.16**  
v3.0  
实现了试飞成功，修改了yaw轴逻辑问题，更新了tof解算，优化了降落过程  

**1.17**  
v3.1  
在图像处理部分针对位姿带来的变化增加了补偿，实测系数为1.2（测量误差依旧较大）  

**1.28a 1.28b**  
修正imu先前存在的映射问题  
摘出坐标映射部分至imu.h，方便安装位置的变化  
整理飞控代码，更模块化，状态机控制更集中化  

**1.28c**  
考虑到总钻风帧率50Hz，更改图像处理的周期至20ms  
Tof阻尼现仅在超时时采用  

**1.28d 1.28e**  
统一编码方式至UTF-8  

**1.29a 1.30a**  
第一次调参  
调整README.md的换行操作，在所有行后添加两个空格  
将角速度环结果赋值到motor_out结构体，方便调试   
调整yaw正方向定义，使之符合右手系  
增加方便调试pid，打印各环pid输出值的函数  

**1.30b**  
第二次调参  
修正映射bug  此版本在m7_0的视觉处理部分被注释掉  

**1.30c  1.30d**  
第三次调参  

**1.31a  1.31b  1.31c  2.1a**  
第四次调参  
第一次校准姿态  
调大最大倾角限制  
新的imu位置映射  
质心位置格式改为float  
添加微分项的低通滤波  

**2.1b**  
添加了视觉环调参  
第五次调参  

**2.2a 2.2b**  
第六次调参  
视觉校正增加高度的影响；修正视觉校正参数至1.47  
第一次可以接受的悬停  

**2.2c /beta01**  
第二次可以接受的悬停，较好的悬停  

**3.4a**  
现在m7_0核向m7_1核发送姿态信息，在m7_1核与目标点信息一同下传小车  
现在README按md格式重整，浏览时请使用预览  

**3.4b 3.6a**  
新图像矫正上传，尚待细调  
跨核通信依据发送者拆分成两个数组  

**3.7a**  
新增了发送端通讯 ，建议从cm0初始化和发送 ， cm1由于不知名原因无法发送 ， 通讯含有校验 ， 调用Board_Comm_Send_Data(volatile float *data_array)传入指针即可

**3.8a**  
对齐需要传输的数据，现在所有依赖图像的工作都只在图像更新后处理。  

**3.8b**  
增加了按键与开关功能
## 按键对象 (`dev_key`) 内部状态与变量说明
这些变量经过了硬件消抖和边沿提取

| 变量名 | 数据类型 | 含义说明 | 典型应用场景 |
| :--- | :--- | :--- | :--- |
| **`event`** | `Key_Event_e` | **当前周期的瞬间动作事件**。<br>只有在动作发生的那一个周期（10ms内）会有值，其余时间均为 `KEY_EVT_NONE`。 | 用于触发单次动作。例如：短按切换模式、长按解锁无人机。 |
| **`is_pressed`** | `uint8_t` | **当前的稳定长效状态**。<br>`1` = 当前正处于按住/闭合状态<br>`0` = 当前处于松开/断开状态。 | 用于电平保持类的逻辑。例如：判断拨码开关当前是否打在 ON 的位置。 |

**`event` (动作事件) 的枚举值说明：**
* `KEY_EVT_NONE` (0)：无动作发生（绝大部分时间的默认值）。
* `KEY_EVT_DOWN` (1)：**按下瞬间**（下降沿），刚接触的那一刻触发一次。
* `KEY_EVT_UP` (2)：**松开瞬间**（上升沿），按下时间超过一秒，刚抬起的那一刻触发一次。
* `KEY_EVT_SHORT` (3)：**短按**，按下并松开，且按住的时间小于长按阈值（1秒）时，在松开的瞬间触发一次。
* `KEY_EVT_LONG` (4)：**长按**，按住不动的时间达到设定阈值（1秒）的瞬间触发一次（此时手还未松开）。

---

| 变量名 | 数据类型 | 含义说明 | 典型应用场景 |
| :--- | :--- | :--- | :--- |
| **`press_time`** | `uint16_t` | **已按下的持续时间**（单位：ms）。<br>只要处于按下状态，该值就会不断累加；一旦松开立即清零。 | 用于自定义超长按逻辑（如按住3秒关机：`press_time >= 3000`）。 |

---

**3.8c**  
增加了app文件，将无人机分为飞行模式和调试模式，开关1打到on为飞行模式  

**3.9a**  
增加了图像膨胀处理 ，对线的干扰抑制作用待观察 。 增加了按键调参功能：  
key1短按切换到下一个参数 ， key2短按将选定参数下调5% ， key2长按将选定参数上调5%  
一个PIT不能同时被两个核使用  
考虑到图像膨胀是8邻域的，连通域判定范围不需要修改  

**3.9b 3.9c**  
增加了长宽比判断 ， 用于区分小车与信标  

**3.9d**  
[重要]交换无人机右前和右后旋翼pwm端口  

**3.9e 3.9f**  
设置GPIO端口使能通信  
在赋值小车和目标位置前检查有效亮斑数量  

**3.10a**  
更改了按键调参：按key2调大参数，key3减小参数  
更改了排序函数，现在取面积最大的两个连通域比较 ratio  
调整了threshold为130：在较暗环境下测试，未开启的信标在无人机正下方时处于可见离散状态，在阈值到达250时可见为一点（像素数量为9，实际为1），阈值较低时为三个有效光斑，130左右时为3个无效连通域，且无人机离其稍远时不可见。信标开启时threshold变大使信标缓慢变小，实际影响不大，远距离时亦可见且较大。  


**3.11a**  
增加了视野中只有一个目标时旋转的功能  
增加了丢失目标后记忆功能  

**3.12a**  
更新逐飞库至版本3.9.1[注意：需要更新iar目录文件]  

**3.12b**  
现在debug模式会进行除了电机赋值之外的所有流程  
现在debug屏幕会打印电机输出数据  
整理部分代码和功能的位置，提高可读性  

**3.14**  
本次提交几乎未对代码有改动，仅对3.12b和beta01版本的差异做分析。  
+ 硬件的更改使无人机的重量与分布发生改变，可能导致需要重新调整参数  
+ 在imu.h中“当前安装: 传感器X向前, Y向右, Z向下”两个版本不一致  
+ 调用Flight_Hover_Control_Task。beta1放在20ms的中断内调用。现版本使用一个通讯的标志位，实测调用周期在25ms左右，考虑更改CTRL_DT_CTANG或重新调整参数。  

**3.16c**  
对sort函数做了逻辑更改，目前[0]表示小车相关参数，[1]表示信标相关参数  
发送的data_out[14]即小车收到的uart_data[4]表示：  
+ 0：未识别到任何有效灯  
+ 1：只识别到小车  
+ 2：只识别到信标  
+ 3：同时识别到信标和小车  


**3.16d**  
在sort双侧加入动态阈值  

**3.16e**  
优化图像的二值化，深搜和特征提取函数  
删除遗留的image_send()和fly_ctrl.h遗留的跨核发送函数  
优化减少矫正时三角函数的计算次数  

**3.17a**  
更新摄像头标定值  

**3.18a**  
引入新视觉解算作为无人机定点的参考  

**3.19**  
增加图像环计时器  

**3.20a**  
新环境下再次实现无人机稳定  
供参考的中间一次参数：  
    // 视觉部分  
    Nonline_PID_Init(&pid_image_x, 0.0623f, 0.00f, 0.138f, 0.00f, 100, 15, 6.0f);  
    Nonline_PID_Init(&pid_image_y, 0.0623f, 0.00f, 0.138f, 0.00f, 100, 15, 6.0f);  


**3.21a** 
完善了无人机的搜索部分代码

**3.22a**
完善了无人机的搜索逻辑，增加了flight_hover_contral的看门狗
视觉环加入了积分项和死区处理

**3.23a**
调整了几个参数

**3.24a**
将yaw数据改为累加的数据，即转过180度后继续增加。完善了搜索逻辑

**3.25**
完善了搜索逻辑,验证逻辑没有问题

**3.29**
恢复了飞行功能，减小了最大目标倾角

**3.30**
恢复了飞行功能，重新调整了参数

**4.2a**
调整优化坐标系换算过程中yaw的参与
调整了参数，可以在仍旧画圆的情况下完成转向

**4.2b**
平飞测试：lf：4243  rf:3763
lb:4320   rb:3840
第一次较好的实现了所有功能，加入了静态补偿，完善了偏航角搜索逻辑

**4.3**
未经测试的版本